node_modules/
npm-debug.log
.env
config.local.json
config.json
*.log
.DS_Store
coverage/
.nyc_output/
# dist/ — kept in repo so the HS Experte import can ship the bundled
# bridge without a separate build step. Comment out if you prefer to
# build before each release.
